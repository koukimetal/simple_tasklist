version: '2'
services:
  web:
    build: .
    command: python /webapp/simple_tasklist.py
    ports:
      - "5000:5000"
    volumes:
      - ./webapp:/webapp
    links:
      - db
    extra_hosts:
      - "myhost:172.30.254.1"
  db:
    image: mongo:3.2.9